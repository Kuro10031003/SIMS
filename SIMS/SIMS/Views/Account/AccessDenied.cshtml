@{
    ViewData["Title"] = "Access Denied";
    Layout = "_Layout";
}

<div class="card" style="max-width:520px; margin-top:24px;">
    <div class="card-body" style="text-align:center; padding:48px 32px;">
        <i class="bi bi-shield-lock" style="font-size:48px; color:var(--down);"></i>
        <h2 style="margin-top:16px;">Access Denied</h2>
        <p class="text-muted">You don't have permission to access this page. Your role does not allow you to view this resource.</p>
        <a href="/Account/Logout" class="btn btn-primary mt-2"><i class="bi bi-box-arrow-right"></i> Logout</a>
    </div>
</div>